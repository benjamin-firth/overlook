import chai from 'chai';
const expect = chai.expect;

describe('See if the tests are running and see if my git connection is valid', function() {
  it('should return true', function() {
    expect(true).to.equal(true);
  });
});
